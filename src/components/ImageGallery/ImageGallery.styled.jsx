import styled from "@emotion/styled";

export const Gallery = styled.ul`
    grid-gap: 12px;
    margin-top: 0;
    margin-bottom: 0;
    padding: 0;
    list-style: none;
    display: grid;
    max-width: calc(100vw - 40px);
    
    margin-left: auto;
    margin-right: auto;
`